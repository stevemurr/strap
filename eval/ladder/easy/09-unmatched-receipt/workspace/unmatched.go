// Package receipts reconciles receipt ids from the terminal and acquirer feeds.
package receipts

// Unmatched returns the one id in ids that appears exactly once; every other
// id appears exactly twice. README.md defines the input guarantees.
func Unmatched(ids []int) int {
	panic("not implemented")
}
