// Package inventory reconciles the SKU lists exported by the warehouses.
package inventory

// CommonStock returns the units present in both a and b, each SKU repeated
// as many times as the warehouse with fewer of it holds, sorted ascending.
// README.md defines the ordering and edge cases.
func CommonStock(a, b []string) []string {
	panic("not implemented")
}
