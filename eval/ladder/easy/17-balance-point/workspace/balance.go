// Package balance locates the balance point of a row of parcels.
package balance

// Point returns the leftmost index whose left-hand weights and right-hand
// weights sum to the same value, or -1 if there is none. README.md defines
// the tie-breaking rule and edge cases.
func Point(weights []int) int {
	panic("not implemented")
}
