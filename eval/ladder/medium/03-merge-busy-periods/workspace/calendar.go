// Package calendar combines busy periods from several calendars into a
// free/busy view.
package calendar

// MergeBusy merges overlapping and touching closed periods and returns the
// merged blocks sorted by start. README.md defines the edge cases and the
// performance requirement.
func MergeBusy(periods [][2]int) [][2]int {
	panic("not implemented")
}
