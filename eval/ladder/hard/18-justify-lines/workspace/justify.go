// Package textwrap lays out paragraphs for the fixed-width receipt printer.
package textwrap

// Justify packs words greedily into lines of exactly width bytes and fully
// justifies every line except the last. README.md defines the spacing rules,
// the edge cases and the performance requirement.
func Justify(words []string, width int) []string {
	panic("not implemented")
}
