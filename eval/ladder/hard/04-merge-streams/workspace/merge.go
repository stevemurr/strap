// Package feeds merges per-account activity feeds into one timeline.
package feeds

// Merge returns every element of every stream in one ascending slice. Each
// stream is already sorted ascending. README.md defines the edge cases and
// the performance requirement.
func Merge(streams [][]int) []int {
	panic("not implemented")
}
